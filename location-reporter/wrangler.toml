name = "location-reporter"
main = "src/index.ts"
compatibility_date = "2024-07-22"
compatibility_flags = ["nodejs_compat"]

# Cron trigger schedule definition
[triggers]
crons = ["*/5 * * * *"] # every 5 minutes (Cloudflare supports min 1 min)
